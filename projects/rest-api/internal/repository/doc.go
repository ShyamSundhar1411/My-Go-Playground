// Package repository contains data-access code (database queries,
// external API calls). Define an interface per resource here so
// services depend on an abstraction rather than a concrete driver.
package repository
