// Package service contains business logic. Handlers call into
// services; services call into repositories. Keep this layer free of
// HTTP and database-specific types so it stays easy to unit test.
package service
